tree = struct("stock_id",[], "weight",[], "n_assets", []);
buildDecisionTree(tree);

function buildDecisionTree(albero)
    tabella = readtable("dati.csv");
    for(element in tabella)
        
    end

end